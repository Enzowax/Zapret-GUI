# Сборка PyInstaller
build/
dist/
__pycache__/
*.pyc
*.pyo

# Собранные / крупные / сторонние бинарники — раздаются через GitHub Releases
ZapretControl*.exe
ZapretControl*.zip
TgWsProxy_windows.exe

# Стратегии теперь в presets.json — .bat-файлы стратегий в репозитории не нужны
general*.bat
service.bat

# Локальное состояние
utils/app_config.json
utils/gui_config.json
utils/*.enabled
lists/*.backup
logs/
.write_test.tmp
